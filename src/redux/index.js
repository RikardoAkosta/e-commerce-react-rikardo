const INITIAL_STATE = {

    products: [],
    isLoading: false,
    
}

const reducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        
        default:
            return state;
    }
}

export default reducer;

